version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    groups:
      # Every dependency here is a devDependency, so this collapses routine
      # advisory and version churn into one PR per week instead of one per
      # package. Majors stay ungrouped: they need a deliberate look.
      dev-dependencies:
        dependency-type: "development"
        update-types:
          - "minor"
          - "patch"

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      # The workflow pins actions to commit SHAs, which nobody bumps by hand.
      # Same split as npm: routine bumps together, majors on their own.
      actions:
        update-types:
          - "minor"
          - "patch"
